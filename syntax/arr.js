var arr = ['A','B','C','D'];
console.log(arr[1]);
console.log(arr[3]);

// 배열 안의 값 변경하는 법
arr[2] = 3;
console.log(arr);

// 배열의 길이 를 알아내는 법.
console.log(arr.length);

// 배열의 뒷자리에 변수혹은 내용 추가.(push)
arr.push('E');
console.log(arr);