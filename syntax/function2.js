
// Math.round = 반올림
console.log(Math.round(1.6)); //2
console.log(Math.round(1.4)); //1
 

// return 의 값은 그 즉시 함수는 실행이 종료 되고 그 값을 출력한다는 의미와
// 거기서 함수를 종료시킨다는 특수한 키워드이다.
function sum(first, second){ // parameter - 매개변수라고 함.
    return first+second;
}
 
sum(2,4); // argument- 인자라고 함.